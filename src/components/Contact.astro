---

---

<div class="region" style="--spot-color: var(--gray200);">
  <div
    class="wrapper content flow prose"
    style="--flow-space: var(--space-l);"
    id="contact"
  >
    <div class="flow prose">
      <h2>Reach out</h2>
      <p>
        We specialize in creating domain authority on the web for a small niche
        of markets. We're still putting together a list of these markets we'd
        like to work with but if you are interested in hearing more fill out our
        form and we'll get chat soon.
      </p>
      <form
        name="main"
        netlify-honeypot="bot-field"
        class="switcher"
        method="POST"
        data-netlify="true"
        action="/"
      >
        <input
          type="hidden"
          name="subject"
          value="Form: straightfowardgrowth.com"
        />
        <p class="hidden">
          <label for="bot-field">Do you like delicious treats?</label>
          <input name="bot-field" id="bot-field" />
        </p>
        <span class="stack">
          <label for="name">Name</label>
          <input id="name" name="name" type="text" required />
        </span>
        <span class="stack">
          <label for="email">Email</label>
          <input id="email" name="email" type="email" required />
        </span>
        <span class="stack">
          <label for="tel">Phone number</label>
          <input id="tel" name="tel" type="tel" />
        </span>
        <span class="stack">
          <label for="message">Message</label>
          <textarea name="message" id="message" rows="6" required></textarea>
        </span>
        <button
          type="submit"
          class="button"
          style="--spot-color: var(--color-loud);">Submit</button
        >
      </form>
    </div>
  </div>
</div>
